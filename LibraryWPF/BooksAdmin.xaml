<UserControl x:Class="LibraryWPF.BooksAdmin"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <!-- Define grid rows and columns -->
        <Grid.RowDefinitions>
            <RowDefinition Height="80" />
            <!-- Header or Navigation -->
            <RowDefinition Height="Auto" />
            <!-- Input Form -->
            <RowDefinition Height="Auto" />
            <!-- Buttons -->
            <RowDefinition Height="*" />
            <!-- DataGrid -->
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150" />
            <!-- Sidebar -->
            <ColumnDefinition Width="*" />
            <!-- Main content -->
        </Grid.ColumnDefinitions>

        <!-- Sidebar Buttons -->
        <StackPanel Grid.Row="0" Grid.Column="0" Grid.RowSpan="4" Orientation="Vertical" VerticalAlignment="Top" Margin="5">
            <Button Content="Members" Height="30" Margin="5" Click="MembersNav_click" Name="MembersNav"/>
            <Button Content="Books" Height="30" Margin="5" Click="BooksNav_click" Name="BooksNav"/>
            <Button Content="Settings" Height="30" Margin="5" Click="SettingsNav_click" Name="SettingsNav"/>
            <Button Content="Logout" Height="30" Margin="5" Click="LogoutButton_Click" Name="LogoutNav"/>
        </StackPanel>

        

        <!-- Buttons -->
        <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Left" Margin="10">
            <DockPanel Margin="10">
                <Button Content="Add" Width="100" Margin="0 5 5 5" Click="AddBookButton_Click" Name="CreateButton"/>
                <Button Content="Update" Width="100" Margin="5" Click="UpdateBookButton_Click" Name="UpdateButton"/>
                <Button Content="Delete" Width="100" Margin="5" Click="DeleteBookButton_Click" Name="DeleteButton"/>
                <TextBox x:Name="SearchBox" Width="150" Height="25" VerticalAlignment="Center" Margin="10,0,10,0"/>
                <Button Content="Search" Width="80" Click="SearchButton_Click"  Margin="5" Name="SearchButton"/>
            </DockPanel>
        </StackPanel>

        <!-- DataGrid -->
        <DataGrid Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Grid.RowSpan="3" x:Name="BooksDataGrid"
                  AutoGenerateColumns="True"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch"
                  Margin="10" />
    </Grid>
</UserControl>
