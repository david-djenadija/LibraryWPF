<UserControl x:Class="LibraryWPF.LoginControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:LibraryWPF"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid x:Name="LoginGrid" HorizontalAlignment="Center" VerticalAlignment="Center" Width="300">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Label Content="Username:" Grid.Row="0" Grid.Column="0" Margin="10" VerticalAlignment="Center" />
        <TextBox materialDesign:HintAssist.Hint="Enter your name" Name="username_text" Grid.Row="0" Grid.Column="1" Margin="10" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />

        <Label Content="Password:" Grid.Row="1" Grid.Column="0" Margin="10" VerticalAlignment="Center" />
        <PasswordBox Name="password_text" Grid.Row="1" Grid.Column="1" Margin="10" PasswordChanged="PasswordBox_PasswordChanged" />

        <Button Content="Login" Command="{Binding LoginCommand}" Grid.Column="1" Grid.Row="2" Width="100" />
    </Grid>
</UserControl>
